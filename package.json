{
  "name": "pjn-scraper-argentina",
  "version": "1.0.0",
  "description": "Sistema de gestion judicial PJN Argentina",
  "main": "server.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "cd dashboard && npm install && npx vite build && cd .. && npx playwright install --with-deps chromium",
    "scrape": "node scraper.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "express": "^4.21.0",
    "multer": "^2.1.1",
    "node-cron": "^3.0.3",
    "playwright": "^1.49.0"
  }
}
